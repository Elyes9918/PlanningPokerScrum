export interface Task {
    id: string;
    title: string;
    body: string;
    author:string;
}

export interface Etask {
    id: string;
    title: string;
    body: string;
    author:string;
    estimation:number;
}